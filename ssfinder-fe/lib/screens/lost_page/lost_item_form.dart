import 'package:flutter/material.dart';

class LostItemForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('분실물 등록')),
      body: Center(child: Text('분실물 등록 페이지')),
    );
  }
}
